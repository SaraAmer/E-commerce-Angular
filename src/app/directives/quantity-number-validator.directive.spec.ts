import { QuantityNumberValidatorDirective } from './quantity-number-validator.directive';

describe('QuantityNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new QuantityNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
